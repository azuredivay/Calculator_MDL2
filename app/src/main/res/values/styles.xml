<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="splashScreenTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/primary</item>
    </style>
    <style name="maintheme" parent="Theme.AppCompat.NoActionBar">

    </style>

    <style name="OperatorButton" parent="android:Widget.Button">
        <item name="android:background">#e5e5e5</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:textSize">18sp</item>
        <item name="fontPath">fonts/segmdl2.ttf</item>
        <item name="android:color">#000000</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="NumPad" parent="OperatorButton">
        <item name="fontPath">fonts/segoeui.ttf</item>
        <item name="android:background">#f5f5f5</item>
        <item name="android:tag">"digit"</item>

    </style>

    <style name="taskbarbutton" parent="OperatorButton">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_margin">0dp</item>

    </style>

</resources>
